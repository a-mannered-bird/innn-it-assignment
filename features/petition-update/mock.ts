/**
 * Throwaway stand-ins for data the real backend would supply. Delete this
 * file when the feature is wired to an API; consumers import it directly so
 * every usage stays greppable.
 */

/** The petition starter's display name, which would come from the session. */
export const DEFAULT_AUTHOR = "Petra Petitionsstarterin";
